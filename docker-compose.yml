version: '3.8'

services:
  qa_agent:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: qa_agent_app
    ports:
      - "8501:8501"
    env_file:
      - .env
    environment:
      - PORT=8501
      - BACKEND_PORT=8000
    volumes:
      - ./:/app
